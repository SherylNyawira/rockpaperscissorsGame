const age = 18;
const canVote = age >= 18 ? 'Yes, you can vote' : 'No, you cannot vote';
console.log(canVote); // Outputs: "Yes, you can vote"
 
const a = 10;
const b = 20;
const result = a > b ? console.log('a is greater') : console.log('b is greater');